---
updated: 2026-04-21
---
# Vault 总索引

> 这里是 agent 提炼后的知识入口。原始镜像在 `mirror/`（只读），整理产出在本目录。
> 最高价值在 `cross-project/` —— 没有它用户不如直接翻 mirror。

## cross-project/（跨项目模式 — 最高价值）

### patterns/

（agent 发现后在此填充）

### decisions/

（agent 发现后在此填充）

### gotchas/

（agent 发现后在此填充）

## synthesized/（单项目提炼）

（首次同步某个项目后，agent 会在 `synthesized/<project>/` 下建子目录）
